version: 1
categories:
  budget:
    status: gating
    min_precision: 0.95
    min_recall: 0.95
    min_severity_accuracy: 0.90
  activity:
    status: shadow
    min_precision: 0.90
    min_recall: 0.85
    min_severity_accuracy: 0.85
  pacing:
    status: planned
    min_precision: 0.90
    min_recall: 0.85
    min_severity_accuracy: 0.85
  logistics:
    status: planned
    min_precision: 0.90
    min_recall: 0.85
    min_severity_accuracy: 0.85
  documents:
    status: planned
    min_precision: 0.90
    min_recall: 0.85
    min_severity_accuracy: 0.85
  weather:
    status: shadow
    min_precision: 0.90
    min_recall: 0.85
    min_severity_accuracy: 0.85
  safety:
    status: shadow
    min_precision: 0.90
    min_recall: 0.85
    min_severity_accuracy: 0.85
  # Reference-only: extraction accuracy is evaluated separately via
  # _run_extraction_baseline() in snapshot.py using its own F1 system.
  # The real gate is extraction_health.blocks_ci in the snapshot.
  extraction:
    status: shadow
    min_precision: 0.85
    min_recall: 0.85
    min_severity_accuracy: 0.80
  # Reference-only: pipeline accuracy is evaluated separately via
  # _run_pipeline_baseline() in snapshot.py using its own accuracy system.
  # The real gate is pipeline_health.blocks_ci in the snapshot.
  pipeline:
    status: shadow
    min_accuracy: 0.80

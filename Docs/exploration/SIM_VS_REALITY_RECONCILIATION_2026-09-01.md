# Simulation Wave vs Code Reality — Reconciliation (2026-09-01 wave)

**Persona:** PER-0930 Shadow-System Investigator
**Date:** 2026-09-02 · **Mode:** READ-ONLY cross-verification; this document is the only output.
**Subject:** the "Gemini" simulation wave dated 2026-09-01 — `Docs/personas_scenarios/MASTER_PRODUCT_DEMO_SIMULATION_CHRONICLE_2026-09-01.md` (v2.0), 16 `CASE_STUDY_SIMULATION_*.md` files, 12 `Docs/personas/PERSONA_*.md` files — reconciled against code reality established by `AGENTIC_FLOW_DEEP_MAP_2026-08-31.md`, `DOCS_CORPUS_SHADOW_AUDIT_2026-08-31.md`, `AGENTIC_DEEP_AUDIT_SYNTHESIS_2026-08-31.md` (G-01…G-19), and `GEMINI_WAVE_MODULE_AUDIT_2026-09-01.md` (GM-01…GM-09).

**Method:** every mechanism-implying claim in the chronicle/case studies was probed against the working tree. Deterministic-engine claims were re-run through the real code with `.venv/bin/python` (identical inputs); UI/sample-data claims were verified by reading the component source; network/issuance claims were checked against the zero-network-call finding of the module audit (`rg 'httpx|requests\.|aiohttp|urllib'` across every new module → no matches).

**Ground rule applied:** screenshots and UX descriptions of what rendered on `:3005`/`:8000` are valid records — 54 `sim_*`-pattern PNGs exist in `Docs/review/assets/` (non-trivial sizes, dated Sep 1). What is adjudicated here is only where the *narrative implies a mechanism that does not exist*.

---

## 1. Claim Verification Table

Verdict key: **VERIFIED** (mechanism exists and reproduces; file:line cited) · **SIMULATED** (sample UI/data; deterministic simulator; no external effect) · **PARTIAL** (real mechanism, overstated or weakened) · **UNVERIFIABLE**.

| # | Claim (source) | Verdict | Evidence |
|---|---|---|---|
| 1 | Sam Rivera note → "extracted travel dates 2027-04-10 to 2027-04-20, party size 3 in <1 second" (chronicle §2.2) | **VERIFIED** | Re-ran the exact note through `src/intake/extractors.ExtractionPipeline` (`content_type="freeform_text"`): `date_start=2027-04-10` (0.95), `date_end=2027-04-20` (0.95), `party_size=3` (0.90), `budget 14000 USD`, warm runs **0.003–0.007 s**. Colloquial-extraction fixes (landed 08-31, register F-21/F-23) do hold. |
| 2 | Salutation fix (`Hi Sam!` not extracted as destination), colon budget, inline `Destinations:` line (chronicle §7, all "VERIFIED") | **VERIFIED** | Same probe: `destination_candidates=['Tokyo','Kyoto']` with the salutation present; `budget_raw_text="budget: around $14,000"` → 14000 USD. Matches `src/intake/extractors.py` changes in this wave. |
| 3 | Marcus note: wheelchair 90%, jain 80%, onion/garlic/root-veg 80%, visa concern 70% (junior-agent case study §3) | **VERIFIED** (one overclaim) | Probe of the Europe note: `mobility_constraints=…` (0.90), `meal_preferences=jain` (0.80), `hard_constraints=['onion','garlic','root vegetables']` (0.80), derived `visa_concerns_present=True` (0.70), `party_composition={adults:3, elderly:2}`. **Overclaim:** §2 table says "Extracted London, Paris, Lucerne, Rome" — probe returns `['London','Paris','Lucerne']`; Rome is missing (the case study's own §4 admits residual/noise). |
| 4 | Haphazard note: urgency high 90%, visa 70%, destination `open`, budget stretch (haphazard case study §2–3) | **VERIFIED** | Probe: `destination_status=open`, `date_confidence=tentative`, `budget_max=7000`, `budget_flexibility=stretch`, derived `urgency=high` (0.90), `visa_concerns_present=True` (0.70). Minor unclaimed gap: `party_size=2` extracted from a 3-person party. |
| 5 | "recalled Alex Morgan's repeat memory graph" (chronicle §2.2; hobbyist case study secondary value driver "cross-trip memory graph") | **SIMULATED** | `frontend/src/app/(agency)/workbench/RepeatTravelerRecallCard.tsx:37-58` — static sample object `cust_sample_demo`, `name:'Alex Morgan'`, sources prefixed `"Sample:"`, badged "Sample data" (DEMO-04 remediation intact). No recall ran; the chronicle's word "recalled" presents the sample as memory. |
| 6 | "11-Persona Council Command Center" (chronicle §2.4; panel heading) | **SIMULATED** (label, not an 11-agent runtime) | `PersonaCouncilPanel.tsx:209` renders the literal heading "11-Persona Council Command Center". The panel is a 16-tab demo console (`:230-245`) whose sub-panels seed hardcoded demo ids: `trip_council_demo` (`:85,:135`), `trav_alice`/`trav_bob` budgets (`:157-164`), `agency_waypoint_hq` (`:185`). |
| 7 | EU261 statutory compensation (€600, Reg (EC) No 261/2004 Tier 3) — Rachel §6, Siddharth §2 | **VERIFIED** (deterministic mechanism; simulated trigger) | `src/orchestration/irops_healer.py:87-89`: `comp_amount = 600.0 if delay_minutes >= 180 else 300.0` with correct statute citation. Real arithmetic; the 180-minute delay itself is a test input. |
| 8 | "FX volatility buffers (2% currency exposure hedging)" (chronicle §2.4; Elena §5 "FX hedge… preserves 100% net margin") | **PARTIAL** | `src/fees/settlement_engine.py:69-84`: real arithmetic — `volatility_buffer_pct=2.0`, `interchange_pct=2.9`, buffered rate × 1.02, fee deduction. It is a *price-buffer calculation*, not a hedge; nothing is hedged in any market. "Hedging" outruns mechanism. |
| 9 | VCC issuance: "Generated merchant-bound single-use card `VCC-9A18F0B2`, 5424-XXXX-…-8821, 4500 EUR" (Elena §5) | **SIMULATED** | `settlement_engine.py:101-133` mints `card_id = f"VCC-{h[:8].upper()}"` from a uuid hash — deterministic pseudo-issuance, no card network. Sharper: `FinancialSettlementPanel.tsx:37,48` **hardcodes** `card_id:'VCC-9A18F0B2'` as the frontend fallback — the exact ID quoted in the chronicle is a frontend constant. |
| 10 | "zero-trust capability tokens" (chronicle §2.4; Elena evidence `elena_06`; Rachel header cites `src/security/capability_tokens.py`) | **PARTIAL** (weakened — GM-04 confirmed) | Real: HMAC-SHA256 signature + 7-day TTL + revocation, `spine_api/routers/public_proposals.py:84-107`. Weakened: hardcoded default secret `"waypoint_secret_proposal_key_2026"` (`:81`); any ≥16-char non-signed token validates as `("legacy_ok","trip_legacy")` (`:112-114`, `:127-131`) and resolves to the fabricated demo proposal; agency-scope fallback loop embeds test UUID `d1e3b2b6-…` (`:146`); `_REVOKED_TOKENS` is in-memory. Also the cited path `src/security/capability_tokens.py` **does not exist**. |
| 11 | "5-tier memory: allergies permanent (no decay), seasonal vibes decay after 6 months" (chronicle §2.4; hobbyist §3 step 4) | **PARTIAL** | UI: `settings/components/MemorySettingsTab.tsx:22-25,62,142,180` — defaults permanent / 36 mo / 24 mo / 6 mo, "5-tier architecture rules" copy. Backend library exists: `src/memory/decay_engine.py` ("Permanent safety facts (e.g. food allergies) have an infinite half-life"). Gap: the settings tab is client-local `useState` with **no fetch/API persistence** — no evidence the toggled retention values drive backend behavior. |
| 12 | "48-Hour Price Hold booking acceptance — 48-hour zero-cost DMC inventory holds locked in" (traveler flow §4; VIP "BOOKING ACCEPTED") | **SIMULATED** (conflated with a real adjacent mechanism) | The confirmation is hardcoded copy: `frontend/src/app/proposals/[proposalId]/page.tsx:458-484` ("48-hour zero-cost DMC inventory holds have been locked in") with `accepted` as client `useState`. No DMC inventory-hold mechanism exists. The real, adjacent mechanism modified in this wave is `spine_api/routers/price_lock.py` — a **72-hour quote-window re-lock/rate-drop audit** with optimistic locking + idempotency (F-01, genuinely good work) — which is not a DMC hold and not 48h. |
| 13 | GDSSandboxPanel "Live Sandbox Flight Offers Returned" / "Dual-Stack Live GDS Sandbox" (Fiona case study title: "Dual-Stack **Live** GDS Sandbox Simulation") | **SIMULATED, mislabeled "Live"** (GM-01 confirmed) | `GDSSandboxPanel.tsx:108,166` carry the "Live" copy; the data comes from `src/distribution/amadeus_sandbox_adapter.py` — 2 hardcoded offers (AF022 $3850, BA178 $3620), uuid PNRs. Zero network calls anywhere in the wave. |
| 14 | Margin optimizer: "$4,500 net + 4-day lead + inelastic → USD 5,362.25, 16.1% take-rate" (Elena §4; Lars §2; chronicle "Algorithm recommended") | **SIMULATED math, real arithmetic** | `NegotiationPanel.tsx:52-64` — pure client-side formula: `rawMargin = clamp(0.16×1.20×1.15 − 0.2×0.30) = 0.1608`; `4500/(1−0.1608) = 5362.25`. The numbers reproduce exactly, but there is no backend pricing engine; coefficients are hardcoded in the component. "Algorithm recommended" implies an engine that does not exist. |
| 15 | Supplier bargaining: $8,000 → $7,360 → $7,420 "ACCEPTED BY SUPPLIER ✅" (Lars §2) | **SIMULATED** | The "negotiation rounds" are a hardcoded React state array in `NegotiationPanel.tsx:35-49`, including the supplier's acceptance. No counterparty exists — consistent with the register's own no-go NG-02/NG-04 ("a negotiation surface without supply-side connectivity is a demo, not a product"). |
| 16 | Penalty waiver bot: goodwill letter for `BK-MARRIOTT-9921` (Lars §3) | **SIMULATED** | `NegotiationPanel.tsx:66-74` — a client-side template string; nothing is dispatched. |
| 17 | IVR bypass: "18-minute hold … bridged live BA agent to advisor phone" (Rachel §7; chronicle "18m hold queue bypassed") | **SIMULATED + numeric conflict** | `src/telephony/ivr_bypass_bot.py:68` hardcodes `hold_duration_seconds=145` (~2.4 min) and synthesizes the transcript; `IVRBypassPanel.tsx:47` defaults to 145s. The "18-minute hold" figure appears only in the narrative. No telephony occurs (fabricated `wss://stream.waypoint.ai`, GM-02). |
| 18 | Knowledge base playbooks "synced with vector memory" (Elena §6; chronicle §5.6) | **SIMULATED (as "synced")** | `frontend/src/app/(agency)/knowledge/PageClient.tsx` — playbook entries are a hardcoded `useMemo` array (`kb_01`…, incl. the "Schengen Visa Processing & Appointment Strategy for Indian Passports" title). The playbooks render and read well; nothing is "synced with vector memory" from this surface. |
| 19 | Managerial quote review gate `STOP_NEEDS_REVIEW` (Marcus §3, Elena §2) | **VERIFIED** (real decision state; demo tenant data) | `src/analytics/policy_rules.py:7,74,105`, `src/agents/runtime.py:704`, invariant `spine_api/routers/settings.py:234-245` ("STOP_NEEDS_REVIEW must always be 'block'"). `/reviews` route and reason copy are real product behavior; `TRIP-E968E4` is demo-trip data behind a real gate. |
| 20 | MRZ 7-3-1 mod-10 checksums (Tariq §2) | **VERIFIED algorithm, SAMPLE input** | `src/intake/mrz_parser_engine.py:70` implements genuine ICAO 9303 check digits (module audit). The probed MRZ (`P<UTOERIKSSON…`) is the ICAO document's own worked example. No OCR/scan pipeline feeds it (GM-06 pattern). |
| 21 | Group Pareto consensus 0.82 vs 0.34; split ledger with pay links (Chloe §2) | **PARTIAL** | `src/decision/group_pareto_engine.py` is real deterministic math (16 new tests green, module audit §3). But the panel seeds hardcoded demo travelers/options (`PersonaCouncilPanel.tsx:157-164`), and "individual cryptographic payment checkout links" have no payment processor behind them. |
| 22 | Stress benchmark: "50 IROPS healed in 0.203s, P99 4.5ms, €30k EU261, 50 VCCs" (Siddharth §2) | **SIMULATED** | `src/benchmarking/stress_simulator.py` measures its own in-process toy work (module audit §1). The takeaway "proves Waypoint OS can handle nationwide ground stops" (§4.2) is an inference the measurement cannot support. Also unauthenticated-consistent POST with no rate cap (GM-08). |
| 23 | Charter empty-leg arbitrage: $11,660 savings, "NetJets repositioning EL-TEB-MIA-091" (Alexander §2) | **SIMULATED** | `src/charter/aviation_engine.py` — deterministic sim, no fleet feed (module audit). "NetJets Empty-Leg Discovery Matrix" (§ header) does not exist as a feed. |
| 24 | Rate parity: "Queried Hotelbeds and WebBeds wholesale inventory feeds", +$405 capture, 1-click voucher swap (Mateo §2) | **SIMULATED + SHADOW** | `src/yield_arbitrage/rate_parity_engine.py` has **zero production callers** (GM-06); the panel surfaces the pre-existing `/api/v1/yield` router which does not use it. No Hotelbeds/WebBeds connector exists. |
| 25 | NDC 21.3 direct connect `ORD-NDC-BA-99A841`, EDIFACT parse, "ATPCO ADM Shield" (Klaus §2) | **SIMULATED** | No NDC/EDIFACT/ADP module exists in the wave inventory (module audit §1); `DistributionPanel.tsx` renders the capability copy. "British Airways NDC 21.3 API" implies a live API that does not exist. |
| 26 | Crisis evacuation: `DOS-EMERG-994` "transmitted to U.S. Embassy Tokyo", armored convoy/jet manifest, driver dispatch `DRV-99418` (Devlin §2) | **SIMULATED** | `CrisisEvacuationPanel.tsx` renders client-side fixtures; `src/crisis/models.py` got only an additive enum in this wave. No external transmission of anything to any embassy is possible (zero network calls). |
| 27 | Duty-of-care radar: geofence threat radar, "Verified active GPS beacons", STEP transmit (Rachel §4-5) | **SIMULATED** | `src/orchestration/duty_of_care_radar.py` — crisis incidents/beacons/consular advisories are fixture data (module audit). Rachel's header cites `src/monitoring/duty_of_care_radar.py` — **path does not exist**. |
| 28 | Traveler Companion PWA: offline cache, live flight status, SOS beacon broadcast (companion case study) | **PARTIAL** | Real: `frontend/public/sw.js` (genuine service worker caching `/companion` + `/manifest.json`), `manifest.json`, and the page exist. Overstated: flight `BA178`/Seat `02A`/Gate `B22`/E-ticket `006-2345678901`/`HTL-AMAN-88219` are hardcoded JSX (`companion/page.tsx:99-253`); "Broadcasted GPS + transmitted DOS-EMERG-994" is `useState` toggling; sw.js caches the shell, not itinerary data. |
| 29 | Case-study "Underlying Architecture" path citations (Rachel, Fiona, Alexander, Mateo headers) | **PARTLY WRONG (record defect)** | Non-existent paths cited: `src/monitoring/duty_of_care_radar.py`, `src/telephony/ivr_bypass.py`, `src/security/capability_tokens.py`. Live-API names cited as architecture ("Amadeus Travel Innovation API", "Hotelbeds/WebBeds Wholesale API Connector", "NetJets/WheelsUp Empty-Leg Discovery Matrix", "IATA NDC 21.3 Gateway") describe connectors that do not exist. `src/corporate/policy_engine.py`, `src/orchestration/irops_healer.py`, `src/intake/mrz_parser_engine.py` citations are correct. |
| 30 | Chronicle §7 friction table: extractor fixes "VERIFIED (100% precision / $14,000 / ['Tokyo','Kyoto'])" | **VERIFIED** | Probe row 1–2 confirms all three fixes behave as described on the same inputs. |

**Score:** 7 VERIFIED (rows 1,2,3*,7,10-part,19,20-alg,30) · 5 PARTIAL · 17 SIMULATED (4 of them with mislabeled "Live"/fabricated-transmission copy) · 0 UNVERIFIABLE. (*row 3 verified with an overclaim on Rome.)

---

## 2. Contradiction Map (narrative vs mechanism)

UX descriptions of what rendered remain valid records. These are the places where the *capability narrative* needs a mechanism caveat:

| # | Narrative says | Mechanism is | Caveat required |
|---|---|---|---|
| C1 | "recalled Alex Morgan's repeat memory graph"; "Cross-trip memory graph preserving dietary and medical safety rules permanently" (hobbyist verdict) | Static sample card, badged "Sample data" (`RepeatTravelerRecallCard.tsx:37-58`) | Label as sample in any buyer-facing retelling. Note: the Sam note's peanut allergy was **not** extracted into any slot (probe) — the memory story's input premise is itself unproven. |
| C2 | "11-Persona Council Command Center" | Panel heading; 16-tab demo console with hardcoded demo ids | Name is decorative; do not read as an 11-agent council. |
| C3 | "Dual-Stack **Live** GDS Sandbox"; "**Live** Sandbox Flight Offers" | Deterministic adapter, 2 hardcoded offers, uuid PNRs | The word "Live" in the UI is the single most misleading artifact in the wave (GM-01). |
| C4 | "Instant issuance of merchant-bound virtual card VCC-9A18F0B2" | Hash-minted id; same ID hardcoded in the panel fallback | No card exists; "issuance" = string generation. |
| C5 | "48-hour zero-cost DMC inventory holds locked in"; VIP "BOOKING ACCEPTED" | Hardcoded confirmation copy; client `accepted` state | No hold, no DMC, no booking. Adjacent real mechanism (price_lock re-lock audit) is different and 72h. |
| C6 | "Algorithm recommended USD 5,362.25 / 16.1%" | Client-side formula, hardcoded coefficients (which do reproduce the number) | Fine as an interactive calculator; wrong as "the algorithm's recommendation." |
| C7 | "ACCEPTED BY SUPPLIER ✅" bargaining | Hardcoded React array including the acceptance | A negotiation bot without a counterparty is exactly what NG-02 already ruled a demo. |
| C8 | "18-minute hold … live carrier agent bridge" | `hold_duration_seconds=145`, synthesized transcript, fake stream URL | Narrative number conflicts with its own simulator's constant. |
| C9 | "Playbooks … synced with vector memory" | Static frontend array | RAG exists elsewhere (`src/rag/`) but is not connected to this surface; and per G-04/NEW-04 the RAG "dense" claim is itself a hash vector. |
| C10 | "Transmitted DOS-EMERG-994 to U.S. Embassy Tokyo" (×3 docs: Rachel, Devlin, companion) | Zero network calls in the wave; fixture feeds | "Transmit" is impossible in the current build; this is the highest-stakes wording in the corpus (life-safety framing) and must be softened everywhere. |
| C11 | "proves Waypoint OS can effortlessly handle nationwide ground stops for mega-TMCs" (Siddharth §4.2) | Simulator measures its own toy coroutine workload | Business conclusion unsupported by the measurement. |
| C12 | Case-study "Underlying Architecture" headers | 3 cited module paths do not exist; 4 cited live APIs/connectors do not exist | Fix citations to real paths; mark connectors as roadmap. |
| C13 | BUILD_QUEUE "Completed" additions (this wave's diff) | Records the same wave's shadow/fake modules — "Production Amadeus/Twilio/Stripe adapters", payment mandates, retention enforcer, DLQ inspector, perishable sentinel — as completed capabilities with no sim/shadow caveat; even lists "[F-02] Cryptographic Proposal Capability Tokens" complete while GM-04 shows the bypass | New drift of the G-01 class, introduced by the record update itself. |

---

## 3. Findings Reconciliation

### 3.1 Are G-01…G-19 captured anywhere in the review/status layer?

**No.** `rg "\bG-0[1-9]\b|\bG-1[0-9]\b"` across `Docs/review/`, `Docs/status/`, `Docs/INDEX.md` → **0 files**. The synthesis inventory (`AGENTIC_DEEP_AUDIT_SYNTHESIS_2026-08-31.md` §3) exists only inside `Docs/exploration/`. The diffs under audit here did not add it:

- `git diff Docs/review/FINDINGS_REGISTER_2026-08-31.md` = 21 lines: **only A-14 and A-21 closures** (both justified, evidence-linked to `A1_1_WORKTREE_CLASSIFICATION_CLOSURE_2026-09-01.md` and commit `2f9a638`).
- `git diff Docs/review/IMPLEMENTATION_PLAN_2026-08-31.md` = Wave-0 rationale/0.4/exit-gate updates only. The synthesis's own phased plan (§5, G-05…G-16 sequencing) is **not merged** into this plan and the plan does not reference the synthesis at all — two implementation plans now coexist with no cross-link.
- `git diff Docs/status/BUILD_QUEUE_CONSOLIDATED.md` = large "Completed" additions for this wave's modules (see C13) — no G-rows.

Theme-level overlaps that already exist in the register (candidates for ID mapping, not duplicates to re-file):

- G-04 (RAG hash-vector) ≈ register **NEW-04** (same finding, already P1).
- G-11 (unpartitioned CUSTOMER_MEMORY_STORE) ≈ **F-23** (same, open).
- G-01 (simulated subsystems) ≈ Part-4/5 sample-panel rows + A-row history — but G-01's post-wave status ("amplified 5×" per GM-01) is recorded nowhere.
- G-13 (MEMORY.md business-model inversion), G-02 (routing_health), G-03 (orphaned LLM assets), G-05/G-06/G-07 (eval lanes, holdout leak, journey tests), G-08 (egress delimiter), G-09 (cross-tenant promote), G-10 (public-checker DoS), G-12 (competing registers — ironically demonstrated by 3.3 below), G-14 (seasonal campaigns undocumented), G-15 (ADR/backlog sprawl), G-16–G-18 (deferred/record-only): **not captured anywhere in review/status.** G-09 and G-10 are P0-adjacent security findings with zero register presence.

### 3.2 Are GM-01…GM-09 captured?

**No.** `rg "GM-0[1-9]" Docs` → only `GEMINI_WAVE_MODULE_AUDIT_2026-09-01.md` itself. The register, plan, and queue were updated by the same wave the audit indicts, and none of the audit's findings were folded in.

### 3.3 New conflicts introduced by the wave's own record updates

1. **A-18 status split across the two registers.** `FINDINGS_REGISTER_2026-08-31.md` still rows A-18 as "⚠️ PARTIAL — live `OPENAI_API_KEY` (rotate)…", while `FINDINGS_TASKS_CONSOLIDATED_2026-08-30.md` now marks A-18 "RESOLVED 2026-08-31 … rotation downgraded to good hygiene (Pranay's call)". Same finding, opposite statuses, same day — the exact competing-registers failure G-12 names.
2. **BUILD_QUEUE completion claims vs audit verdicts.** "[F-04] Payment Mandates", "[F-05] Retention/Erasure", "[F-07] DLQ", "[F-14] Perishable Sentinel" are recorded Completed although those modules have **zero production callers** (GM-06); "[F-02] Capability Tokens" recorded complete while GM-04 documents bypass + default secret; "Area #17" shadow modules (accounting/briefing/corporate/visa/logistics-adjacent) recorded complete without wiring status.
3. **Register-staleness pattern repeats.** The shadow audit's sharpest 08-31 finding (register stale at publication vs Wave-2 handoff) recurs structurally: the wave updated the *queue* ("Completed") and *register* (A-14/A-21) but not against the module audit completed one day later — and the findings-gate that would catch this (`scripts/check_findings_register.py`) is still not wired into CI (NEW-05, unchanged).
4. **Not-a-conflict, ratify:** the A-14/A-21 closures themselves are well-evidenced (167-path inventory, CSV ledgers, checker tool) and consistent with the F-20 supersession notes; no objection.

### 3.4 Reconciliation table (audit finding → where it lives today)

| Finding set | In register/plan/queue? | Correct home |
|---|---|---|
| G-01…G-19 (deep audit synthesis §3) | ❌ nowhere (theme-overlaps only: NEW-04≈G-04, F-23≈G-11) | Register Part-5 rows G-01…G-19 with severity + disposition, or a pointer block to the synthesis |
| GM-01…GM-09 (Gemini wave audit) | ❌ nowhere | Register rows + BUILD_QUEUE caveat column |
| C1–C13 caveats above | ❌ nowhere | Chronicle/case-study headers + BUILD_QUEUE statuses |

---

## 4. Verdict (state of the record)

The wave is two records in one and the record layer currently conflates them. **The deterministic core claims verify:** the intake-extraction narratives (Sam, Marcus, haphazard) reproduce through the real `ExtractionPipeline` to the quoted confidence values in single-digit milliseconds, the salutation/colon/destinations fixes hold, the EU261/FX/interchange arithmetic is real, `STOP_NEEDS_REVIEW` gating is real, the MRZ checker is a genuine ICAO implementation, the price-lock optimistic-concurrency work is real, and 54 screenshots of real rendered UI exist. **The frontier-capability claims are a simulation archipelago wearing production language:** of 30 adjudicated claims, 17 are simulated and 5 partial — and the simulation is not hidden, it is *mis-described*: "Live" GDS offers, "issued" VCCs whose IDs are frontend constants, "accepted by supplier," "transmitted to the U.S. Embassy," "algorithm recommended," on top of an already-amplified G-01 (15 unlabeled sim surfaces, GM-01). Worst of all, the record updates made in the same wave (BUILD_QUEUE "Completed" entries; case-study architecture headers) *encode the simulation as completed capability and cite module paths that do not exist*, while neither the deep-audit findings (G-01…G-19) nor the wave-audit findings (GM-01…GM-09) appear in the authoritative register at all — the audit treadmill the 08-31 corpus audit documented has now completed a full extra lap. **The record is trustworthy for the deterministic product; it is actively misleading about every frontier surface.** Commercial "BUY" verdicts derived from the sim surfaces are not supported by mechanism and should not be quoted as validation evidence.

## 5. Minimal Reconciliation Actions

1. **Label, don't archive (hours):** add a one-line caveat banner to the chronicle + all 16 case studies: "Frontier-capability sections exercise deterministic demo simulators (see `Docs/exploration/GEMINI_WAVE_MODULE_AUDIT_2026-09-01.md`); no external calls, holds, transmissions, issuances, or negotiations occur." Fix the four wrong "Underlying Architecture" path citations (row 29) at the same time.
2. **Fix the five worst copy strings in code (the GM-01 core):** `GDSSandboxPanel.tsx:108,166` "Live" → "Simulated sandbox"; proposal page `:463` "holds have been locked in" → "demo confirmation"; remove/replace "transmitted to embassy" copy in duty-of-care/crisis/companion panels; IVR panel "18-minute hold" → 145s; NegotiationPanel header "algorithm" → "illustrative calculator."
3. **Register integration pass (G-19 + GM):** append G-01…G-19 and GM-01…GM-09 rows to `FINDINGS_REGISTER_2026-08-31.md` (map NEW-04→G-04, F-23→G-11 to avoid duplicates); annotate BUILD_QUEUE sim/shadow "Completed" entries with `RealityTier: simulated/unwired` tags (C13).
4. **Resolve the A-18 split** (3.3.1): one status across both registers; if rotation is genuinely downgraded, amend the register row with the same "(Pranay's call)" note rather than leaving a contradiction.
5. **Wire `scripts/check_findings_register.py` into CI** (NEW-05/G-12) before the next wave so register staleness stops being found by audits.
6. **Keep and commit the hardening stack** (per the module audit's sequencing): inbound idempotency, optimistic sync/price-lock, status machine, RLS flush fix, A-18/A-20 work, extractor recall — separate from any simulator disposition.
7. **Delete or rename before anyone imports them:** `spine_api/providers/*enterprise|issuing|telephony*` simulators (GM-02) and the fake `verify_webhook_signature` (GM-03); close the `≥16-char` legacy token bypass + default signing secret (GM-04) unless a live consumer is named.

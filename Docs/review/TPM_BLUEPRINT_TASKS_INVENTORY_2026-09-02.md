# TPM Blueprint Wave — Findings & Tasks Inventory (Explore / Implement)

**Date:** 2026-09-02
**Sources:** TPM blueprint mapping (`Docs/TPM_TRAINING_BLUEPRINT_PRODUCT_MAPPING_2026-09-01.md`), implementation handoff (`Docs/review/TPM_BLUEPRINT_TASKS_IMPLEMENTATION_HANDOFF_2026-09-01.md`), review cycles 1–2 findings, role-panel outputs (Operator/Skeptic/Champion/Executioner/Outsider/Grounding), shared-tree engagement.
**Purpose:** one candidate inventory of everything explicit (already surfaced + documented) and implicit (surfaced by this work, not yet articulated). This doc does NOT replace the register — items graduate into `Docs/review/FINDINGS_REGISTER_2026-08-31.md` via `scripts/check_findings_register.py` when picked up.
**Already shipped this wave (not re-listed as tasks):** F-27 merge precedence, F-28 intake idempotency, F-29 status invariant, N-4 lifecycle read-model — see handoff.

---

## Section 1 — IMPLEMENT candidates (scoped, evidence-backed, additive)

| # | Task | Size | Priority | Source / evidence |
|---|---|---|---|---|
| I-1 | **Server-side escalated queue**: backend trips-list join with `TripRoutingState` so `?filter=escalated` is server-accurate under pagination; renders `primary_assignee`/`escalation_owner`/`handoff_history` (all persisted, zero UI today). Current FE chip is a client-side slice over the workspace list only. | M | P2 | Operator lens; handoff §7; `routing_service.py`, `management_queue` synthetic assignee has no surface |
| I-2 | **Approval-drift trigger + freshness card**: quote `expired` and `price_lock_expires_at` (72h) never feed trip state; `POST /trips/{trip_id}/reassess` exists but nothing fires it. Add expiry→reassess→re-approval loop + small card on Quote Assessment reading existing price-lock audit endpoints. | M | P2 | Operator lens; `price_lock.py:7-9`; execution-side story stays gated by F-14/EX-14 |
| I-3 | **Allowed-actions panel**: grow `DecisionTab.tsx:47-51` decision_state label map into a per-state allowed/forbidden/next list honoring existing gates (`canAccessOpsWorkspace`, `GENERATE_ALLOWED_STAGES`, autonomy policy). Pure read-model growth; the blueprint's per-state 4-question contract made visible. | S | P2 | Operator lens; blueprint L3/L4 |
| I-4 | **Unify needs-information vocabulary**: one concept lives in three places (inbox lead status, `follow_up_status` pending/snoozed/completed, `ASK_FOLLOWUP` decision state). Define one canonical read-model function, project the others. | M | P2 | Operator lens; no backend behavior change required for the read-model slice |
| I-5 | **Recovery queue**: `feedback_reopen`/`IN_RECOVERY` banner renders only when the operator already has the trip open — no aggregated queue exists. Add a recovery view (read-model first, same pattern as escalated chip). | S | P3 | Operator lens; workbench `PageClient.tsx:943-967` |
| I-6 | **Honest concierge responses**: `messaging_webhooks.py` returns fabricated flight/hotel/emergency copy (Rome AZ601, Hotel de Russie, fake hotline) — textbook fabricated-truth class from the launch audit. Replace with honest placeholder/no-data responses or wire to real trip data. | S | **P1** (reality-boundary) | Found during N-1 wiring; launch-audit defect class |
| I-7 | **Runner env fallback for direct pytest**: direct `pytest` now requires exporting `JWT_SECRET`/`PROPOSAL_SIGNING_KEY`/`PUBLIC_CHECKER_AGENCY_ID` (import-time requirement). Add a conftest-level dev-only fallback (mirroring `scripts/run_backend_tests.sh`) so bare pytest runs don't ERROR confusingly. | S | P3 | Session friction; `public_proposals.py:109` import-time guard |
| I-8 | **Move `_SYNC_PROMOTABLE_FROM` into `core/trip_status.py`**: the optimistic-sync promotable vocabulary is a router-local constant; when the status machine matures it belongs beside `INTAKE_BLOCKED_STATUSES` as a named, tested set. | S | P3 | Cycle-2 fix B follow-through |

## Section 2 — EXPLORE candidates (research + document → then decide)

| # | Topic | What to research/document | Priority | Source |
|---|---|---|---|---|
| E-1 | **Typed-enum trip-status enforcement** | Collect real status distribution via the derived read-model (N-4) in dogfood use; then decide the Literal/enum cut + legacy alias table for `contract.py:1451`. Gate: don't legislate states from a whiteboard. | P1 (gated on data) | Mapping doc discipline; N-2 slice 2 |
| E-2 | **analytics JSON→JSONB migration** | `trips.analytics` is `JSON` (discovered in cycle-2 fixes) — no in-SQL merge/index operators. Explore migration (data shape, lock impact, dual-write window) and what it unlocks (in-SQL `_extra` merges, GIN indexing). Requires migration care on the shared test DB. | P2 | Session discovery; `models/trips.py:70` |
| E-3 | **Multi-worker idempotency backend** | Deployment topology decision → SQL (backend exists in `src/agents/idempotency.py`, parallel-built) vs Redis seam; document when single-process stops being honest. | P2 | N-1 docstring seam; cycle-2 C fix |
| E-4 | **Actor identity on provenance/history** | `status_history`/`_field_provenance` record self-asserted actor labels. Explore jointly with F-03 (approval identity) — JWT-subject + artifact-hash binding; do not build separately. | P1 (with F-03) | Cycle-1 design note; F-03 |
| E-5 | **Per-fact freshness policies** | Blueprint refinement (Outsider): "recheck any fact whose cost of staleness exceeds the cost of rechecking" — build a per-fact-class policy table (fares, visa rules, passport validity, hotel rates, preferences) instead of one ceremonial recheck. Feeds F-14/EX-14 design. | P1 (feeds F-14) | Mapping doc §6 leapfrog; Outsider lens |
| E-6 | **Approval-as-scopes model** | Approval is a permission set (airline/fare ceiling/reserve-not-purchase/cancellation terms), not one event. Document the scope model; it is what makes `AuthorityTier` binding meaningful. Product decision required. | P1 (product decision) | Outsider lens; mapping doc §4 defer |
| E-7 | **AuthorityTier shadow-mode audit** | Agreed adoption path: run the dormant matrix in audit-only mode (log would-be denials) for a release cycle before enforcing at money-touching boundaries. Design the shadow report. | P2 (when connectivity nears) | Mapping doc §4; cycle-1 review |
| E-8 | **Server-side TripLifecycleState persistence** | 12-month horizon: supersede the six-way client inference with one persisted, server-derived state. Needs read-model distribution data + I-1/I-3 usage first. | P2 (12-mo) | Operator horizon; mapping doc §6 |
| E-9 | **IN_TRIP / disruption mode wiring** | `disruption_radar.py`, `crisis_ops.py`, `price_lock.py` are standalone endpoints; nothing marks a trip IN_TRIP. Explore what input signals should flip the state and what the traveler-facing surface becomes. | P3 (24-mo) | Operator lens; mapping doc §6 |
| E-10 | **Survey→memory feedback loop** | Feedback router captures surveys + supplier scorecards but the loop into customer memory isn't closed (explicit corrections / implicit signals / outcomes per blueprint L20). Map the write path + trust gating. | P2 | Grounding C12; `feedback.py` |
| E-11 | **Epistemic status operator surface** | EX-02 (tracked): ambiguity preservation as first-class ("around Oct 5" is accurate conversation data). Explore the UI form of ASSUMED/UNKNOWN on trip fields alongside the merge-provenance already shipped. | P2 | Outsider lens; EX-02 |
| E-12 | **Status alias-table expansion** | `normalize_trip_status` covers only in_progress/new/active; unknowns pass with warning (by design). Collect warned values from logs to grow the alias set evidence-first. | P3 | N-2 module; L21 telemetry |
| E-13 | **packet_version single-home** | Version rides top-level (file) + `analytics._extra` (SQL). Document whether to consolidate into `_extra` for both when the typed state machine lands, or keep dual-carriage deliberately. | P3 | Cycle-2 A fix note |
| E-14 | **Learning-thread completion (non-product)** | Finish the four-question contracts (true/allowed/forbidden/out-transitions) for all 11 blueprint states as an architecture doc — doubles as the spec input for E-8 and the ChatGPT study artifact. | P3 (learning) | Mapping doc §7 answer-key |

## Section 3 — Already tracked; do NOT re-register (themes with register IDs)

- **Money/token integrity:** F-01 (price-lock RMW), F-02 (public proposal tokens), F-03 (self-asserted signoffs), F-04 (payment mandate ledger), F-05 (jurisdiction SLA enforcement), F-07 (poisoned jobs).
- **Truthfulness/epistemics:** NEW-01 (per-field authority inversion), NEW-02 (silent-wrong extraction — P0 class), F-22 (authority mislabeling), F-13 (trust-weighted memory).
- **Deadlines/perishables:** F-14, EX-14 (E-5 feeds these).
- **Connectivity/reality tiers:** R-06→A-03 (RealityTier vs tool connectivity), NG-01..04 (booking-side defers).
- **Platform/process:** F-09 (run-ledger disk-only), F-08 (locking semantics), F-06 (audit-chain gaps), F-11 (subagent spend), F-12 (SSE caps), F-17/F-19 (FE debt, phantom failures), A-04..A-12, R-09..R-16, NEW-03 (retirement gate), NEW-05 (register gate in CI), NEW-06 (customer-memory FE wiring), NEW-07 (lease heartbeat), F-20..F-26 (demo-wave items; F-21 open P1).
- **No-go (binding):** booking sagas, change-request lifecycle, 1M/month scaling taxonomy, dual-writer locking beyond tracked items, AuthorityTier binding-as-build before connectivity (mapping doc §4 + NG-01..04).

## Section 4 — Suggested order (next wave)

1. **I-6** (honest concierge — small, reality-boundary P1).
2. **I-1 + I-2** (escalated queue server-side; approval-drift trigger) — highest operator real-world value.
3. **I-3 + I-4** (allowed-actions panel; needs-information unification) — read-model growth, no backend risk.
4. **E-1 + E-12** data collection begins automatically once I-3 ships (dogfood usage).
5. **E-4/E-5/E-6** as the decision bundle before any money-touching connectivity work (all three feed F-03/F-14/R-06 design).

---

## Section 5 — Exploration wave outcomes (2026-09-02 → 09-04)

All 14 candidates explored and documented. E-5/E-9/E-10 first got compact primary-authored versions during a quota window, then received **full subagent deep passes (2026-09-04) that corrected several compact-v1 claims** — the corrections are the strongest evidence for the verify-before-trusting-a-summary discipline:

| Candidate | Doc | Headline outcome |
|---|---|---|
| E-1 | `Docs/exploration/E1_TYPED_ENUM_STATUS_ENFORCEMENT_EXPLORATION_2026-09-02.md` | **Live SQL distribution captured** (assigned 18,527 / new 1,752 / in_progress 853 / incomplete 132 / active 76 / completed 13 / cancelled 2). Recommends 12-value enum matching frontend `STATUS_TO_STATE`. New defects: `active`/`archived` trips invisible in both workspace and inbox views; `metrics.py` booked-revenue reads a status **no writer ever sets** (structurally zero); `normalize_trip_status` has zero callers. |
| E-2 | `Docs/exploration/E2_ANALYTICS_JSON_TO_JSONB_MIGRATION_2026-09-02.md` | One-statement reversible migration designed (`ALTER ... TYPE JSONB USING analytics::jsonb` + lock_timeout); ~21.6k rows / 15 MB; all 2,854 non-null values are objects; alembic wrong-DB incident already fixed (LR-B03). New PII caveat: `_fold_unmapped_updates` routes unmapped keys into `_extra` **unencrypted** and privacy_guard ignores deep analytics fields. |
| E-3 | `Docs/exploration/E3_MULTI_WORKER_IDEMPOTENCY_BACKEND_2026-09-02.md` | **Register correction: SQL idempotency backend is NOT orphaned** — `SPINE_API_IDEMPOTENCY_BACKEND=sql` pinned in compose/fly/render; PT-08 "PARTIAL" framing is stale. Real gaps: no startup assertion tying workers>1/production to backend; memory backend loses records on restart (retry-after-crash duplicate with zero concurrency); webhook dedup call site dormant (no route reaches it); stale "in-process" comments at both call sites. Redis IS in the stack (deliberate non-goal, not absent). |
| E-4 | `Docs/exploration/E4_ACTOR_IDENTITY_BINDING_F03_2026-09-02.md` | **JWT-subject binding pattern already exists in-repo** (`assignments.py:100-161`, `confirmations.py`) — F-03 is a failure of application, not capability. **Worse-than-F-03 discovery: `corporate_policy.py` routes have NO JWT auth at all** (X-Agency-ID header + TEST_AGENCY_ID fallback) — unauthenticated override approval. Optimistic-sync has no FE caller yet → retrofit is cheap. |
| E-5 | `Docs/exploration/E5_PER_FACT_FRESHNESS_POLICIES_2026-09-02.md` (deep pass v2) | 10-class policy table, deepened + **corrected**: price-lock re-lock is no longer blind RMW (conditional optimistic guard — F-01 needs re-scoping); an FX sentinel exists (`fx_sentinel.py`, DETERMINISTIC_PREVIEW); passport is NOT in packet facts (booking-collection payloads) and validity **is** enforced (180-day rule, `constraint_engine.py:237-260`); `perishable_sentinel.py` (the F-14 engine) exists but is orphaned, zero callers; **two undated visa tables** (`visa_timeline` + `visa_radar`); split-brain: `social_inbound.py:122` writes `price_lock_expires_at` top-level while the sentinel reads `strategy`; insurance CFAR bug computes `now+14d` ignoring `deposit_date`. |
| E-6 | `Docs/exploration/E6_APPROVAL_AS_SCOPES_MODEL_2026-09-02.md` | **Six disjoint approval vocabularies, zero linkage** (quote status, review_status, $10k gate w/ self-asserted reviewer, e-sign in process-local registry, 1-click intent, free-text override). ApprovalGrant model (Dimension × CHECK/RESERVE/PURCHASE × Bound × Conditions × Provenance); verdict: **extend** AuthorityTier, not replace; E-4 identity binding is a hard prerequisite for enforcement. Register correction: payment-rails no-go is EX-05, not NG-01. |
| E-7 | `Docs/exploration/E7_AUTHORITYTIER_SHADOW_MODE_DESIGN_2026-09-02.md` | Framing correction: `evaluate_autonomy_dispatch_gate` has **zero production callers** — the live gate is NB02. Shadow design: 3-state env flag, separate shadow ledger (file audit ledger would be evicted by shadow flood — 10k trim + O(n) append), 100% would-denies / 10% allows / 500/hr cap, `tools/authority_shadow_report.py`, flip criteria (<1% would-deny, zero FP classes). New flag: unauthenticated `GET /tokens/{token}/verify`. |
| E-8 | `Docs/exploration/E8_SERVER_SIDE_LIFECYCLE_STATE_2026-09-02.md` | Derived-on-write at the five TripStore guard choke points recommended. Verified drift locked as pre-work: FE matches ghost values no backend emits; three competing SLA implementations; `GET /api/stats` has **no backend route**; routing-escalated invisible to lifecycle. 12-mo build gated on E-1 collector; 6 unblocked tasks (~3 days). |
| E-9 | `Docs/exploration/E9_IN_TRIP_DISRUPTION_WIRING_2026-09-02.md` (deep pass v2) | Derived-window signal stands (persisted flip has no producer/validator/stage vocabulary). **Corrected basis:** canonical date facts are `extracted.facts.date_window/date_start/date_end/date_confidence` — no `start_date`/`duration_nights` anywhere; "specific date phrases often yield no date slots" (absence is first-class). New finds: `GET /alerts` fabricates a CRITICAL alert for every agency trip unscoped; the feedback "48h auto-trigger" is docstring fiction (nothing fires it); `social_inbound.py:141` already implements the `date_start ?? date_window` fallback. |
| E-10 | `Docs/exploration/E10_SURVEY_MEMORY_FEEDBACK_LOOP_2026-09-02.md` (deep pass v2) | **Supplier scorecard is 100% hardcoded demo data** (no aggregation/persistence); **suitability never reads the memory store** — the only memory→decision path is the legacy process-local hydrate (`customer_memory.py:302`), bypassing decay/provenance entirely, so F-13 has two slot points (`retriever.py:70` + hydrate path); gate does not sanitize (persisted summaries raw 250 chars); half-life hardcoded 730d via `ingest_memory`. Concrete write rules delivered: gate math (min 0.75, `0.5·explicit + 0.5·source_weight`), 6 MemorySourceTypes, decay table (730d explicit / 365d outcome / 90d implicit). Loop has no response-ingestion endpoint; survey URL fabricated (joins I-6). |
| E-11 | `Docs/exploration/E11_EPISTEMIC_STATUS_SURFACE_EX02_2026-09-02.md` | Runtime probe: `party_size` from "probably 4 of us" stamped **FACT @0.9**; `packet.assumptions` never populated in production → the unacknowledged-assumption escalation is a **dead policy check**; `CanonicalPacket.to_dict()` doesn't serialize assumptions; `PacketPanel.tsx` recreates F-22 at the UI layer; `SlotValue` type lacks `epistemic_status` entirely. Surface work gated on F-22/NEW-01 label honesty. |
| E-12 | `Docs/exploration/E12_STATUS_ALIAS_EXPANSION_PLAN_2026-09-02.md` | Warning is **doubly dead** (zero callers + no log aggregation anywhere; OTel traces-only and dormant). Seed table: 13 status literals, 8 absent from the alias map (incl. live `assigned` 18,527 rows). Spec: `tools/status_vocabulary_report.py` (read-only GROUP BY + file-store scan, coverage annotations). |
| E-13 | `Docs/exploration/E13_PACKET_VERSION_SINGLE_HOME_2026-09-02.md` | **Ratify dual-carriage**: production SQL has `packet_version` in **0 of ~21.6k rows** (files = test effluent per R-03) — "two homes" is one home plus a test-double. Option (d) (version inside packet) rejected: extraction replaces packet wholesale → resets version, reopens F-27's window. Hold dedicated-column as an E-2 co-migration rider. |
| E-14 | `Docs/architecture/TRIP_LIFECYCLE_STATE_CONTRACTS_2026-09-02.md` | Four-question contracts for all 13 states (9 live + 4 dormant) with anchors + enforcement-gap matrix (28 rows × exists/enforced/displayed). Live defects: review approval writes `status="delivered"` but read-model knows only `"completed"` (approved trips render as INTAKE chip); **no writer anywhere emits `completed`**; "cannot book before acceptance" is unenforced (no acceptance event exists); `ready_to_quote/ready_to_book` are guard-only vocabulary — never written on the happy path; dual escalation axes resolve independently. |

**New findings surfaced by the wave** — **FILED 2026-09-04 as register Part 4e (F-30…F-40, validator-clean: 127 rows, 0 warnings)**: F-30 corporate_policy unauthenticated (**P1**), F-31 insurance CFAR deposit-date bug, F-32 price_lock_expires_at split-brain, F-33 active/archived view invisibility, F-34 delivered/completed display bug, F-35 booked-revenue metric zero, F-36 feedback reality-boundary cluster, F-37 assumptions pipeline dead end-to-end, F-38 unscoped CRITICAL alert fabrication, F-39 webhook dedup call-site dormancy, F-40 /api/stats missing route. Deep-pass additions + corrections to existing rows (PT-08, F-01, F-13, F-14, F-03 scope, NG-01/EX-05 citation) are recorded in the same part's corrections block — including: price_lock_expires_at write/read split-brain, two undated visa tables, insurance CFAR `now+14d` bug, perishable_sentinel orphaned (F-14's engine already exists), `GET /alerts` CRITICAL fabrication per trip, suitability never reading the durable MemoryStore (sharpens F-13 to two slot points).
